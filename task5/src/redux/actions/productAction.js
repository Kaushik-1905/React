export const addProduct = (product) =>({
    type:"ADD_PRODUCT",
});

export const deleteProduct = (id) =>({
    type:"DELETE_PRODUCT",
});

export const updateProduct = (product) =>({
    type:"UPDATE_PRODUCT",
});